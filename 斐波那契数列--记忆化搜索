#include<bits/stdc++.h>
using namespace std;

long long f[10005];

int fib(long long x)
{
	if(x==1||x==2) return f[x]=1;
	if(f[x]==0) return f[x]=fib(x-1)+fib(x-2);
	return f[x];
}

int main()
{
	int x;
	cin>>x;
	int ans=fib(x);
	printf("%d",ans);
	return 0;
}
